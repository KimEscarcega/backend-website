<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Paking Spaces</title>

    
        <link rel="StyleSheet" href="./css/parkingstylesheet.css">

        <link rel="icon" type="image/x-icon" href="Images/favicon.ico.png">
    
</head>
<body>
    <header>

      <a href="/main">
        <img src="/Images/logo.PNG" alt="select park logo">
        </a>

            
             <ul>
                <li><a href="/account">Account</a></li>
                <li class="dropdown">
                    <a href="/booking" class="dropbtn" >Reservation</a>
                    <div class="dropdown-content">
                      <a href="/future">Reservations</a>
                      <a href="/past">Past Reservation</a>
                    </div>
                  </li>

                <li><a href="/report">Report</a></li>
                <li><a href="/feedback">Feedback</a></li>
                <li><a href="/moreinfo">More Information</a></li>
            </ul>

            </header>

          <main>

            
            <h3>Select an Available Parking Spot</h3>

            <form id="parkinglot" action="/parkinglot" method="POST">
              <input type="hidden" id="spot" name="spot" value="">

                   <!-- checking if the spots are booked they are red   -->

                  <table class="first">
                    <tr>
                      <% for (let i = 1; i <= 10; i++) { %>
                        <% const Bspot = booked.includes(i); %>
                        <td>
                            <% if (Bspot) { %>
                                <button  disabled class="booked"><%= i %></button>
                            <% } else { %>
                                <button onclick="selected('<%= i %>')"><%= i %></button>
                            <% } %>
                        </td>
                        <%#  will output the spots from 1-10 %>
                    <% } %>
                    </tr>

                  </table>
    
                    <table class="second">
                      <tr>
                        <% for (let i = 11; i <= 20; i++) { %>
                          <% const Bspot = booked.includes(i); %>
                          <td>
                              <% if (Bspot) { %>
                                  <button  disabled class="booked"><%= i %></button>
                              <% } else { %>
                                  <button onclick="selected('<%= i %>')"><%= i %></button>
                              <% } %>
                          </td>
                          <%#  will output the spots from 11-20 %>
                      <% } %>
                      </tr>
    
                      
    
                     
                      </table>
            </form>

            

          </main>
            
          <footer>
        
            <p>Created by Group 4 </p> 

    </footer>

    <script src="./js/booking.js"></script>
        
</body>
</html>

